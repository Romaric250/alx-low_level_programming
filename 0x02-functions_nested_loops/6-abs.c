#include "main.h"

/**
 * @n: An integre input
 * Description: the function returns the absolut value of a number.
 * Return: Absolut value of number n
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (n * -1);
}

