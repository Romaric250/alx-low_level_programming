#include <stdio.h>
/**
 * main - Prints the letters of the alphabet in reverse order
(*
 * Return: 0 on success
 */
int main(void)
{
	char letter = 'z';

	while (leter >= 'a')
	{
		
	    putchar(letter);
		
	    letter--;
	}
	putchar('\n');
	return (0);
}
